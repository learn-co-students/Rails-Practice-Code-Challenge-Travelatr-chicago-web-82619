<h1>All Bloggers</h1>

<ul>
  <% @bloggers.each do |blogger| %>
    <li><%= link_to blogger.name, blogger_path(blogger) %></li>
  <% end %>
</ul>

<%= link_to "new blogger", new_blogger_path %>

<%= link_to "all posts", posts_path %>
